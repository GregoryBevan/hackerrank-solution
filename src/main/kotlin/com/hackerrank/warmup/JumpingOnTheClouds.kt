package com.hackerrank.warmup

fun jumpingOnClouds(c: Array<Int>): Int {
    c.forEachIndexed(index, cloudValue -> )
    var jump = 0
    var previous = -1
    for(pos in c) {
        if(previous == -1) previous = 0
        if(previous == 0 && pos == 0) {

        }
    }
}